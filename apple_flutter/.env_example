# 🔧 Google OAuth 설정
# Google Cloud Console에서 생성한 OAuth 클라이언트 ID를 입력하세요
# 형식: 1234567890-abcdefghijklmnopqrstuvwxyz123456.apps.googleusercontent.com
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE

# 🌐 백엔드 API 설정
# 개발 환경에서는 localhost:8080, 배포 시에는 실제 서버 URL로 변경
API_BASE_URL=http://localhost:8080/api

# 📝 설정 방법:
# 1. 이 파일을 .env로 복사하세요: cp .env_example .env
# 2. .env 파일에서 YOUR_GOOGLE_CLIENT_ID_HERE를 실제 클라이언트 ID로 교체하세요
# 3. git에는 .env 파일이 업로드되지 않도록 .gitignore에 추가되어 있습니다

# ⚠️ 보안 주의사항:
# - .env 파일은 절대 git에 커밋하지 마세요
# - 클라이언트 ID는 공개되어도 괜찮지만, 실제 프로덕션에서는 도메인 제한을 걸어두세요
# - 클라이언트 시크릿은 Flutter Web에서는 사용하지 않으므로 여기에 포함하지 않았습니다 